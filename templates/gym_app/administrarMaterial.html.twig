{% extends 'base.html.twig' %}
{% block stylesheets %}{% endblock %}
{% block title %}
	AddMaterial
{% endblock %}
{% block body %}
	<nav>
		<div>
			<a href="{{ path('app_gym',{userName:userName}) }}">Home</a>
			<a href="{{ path('mostrarRutinaActual',{userName:userName}) }}">Rutina actual</a>
			<a href="{{ path('logout') }}" class="btn btn-dark">Cerrar sesión</a>
			<button class="config" id="{{ isAdmin ? 'admin' : 'not_admin' }}">⚙️</button>
			<select onchange="window.location.href=this.value;" hidden class="opciones">
					<option value="{{ path('administrarUsuarios',{userName:userName}) }}">Añadir nuevo administrador</option>
					<option value="{{ path('administrarUsuarios',{userName:userName}) }}">Borrar usuarios</option>
					<option value="{{ path('administrarUsuarios',{userName:userName}) }}">Actualizar usuarios</option>
					<option value="{{ path('administrarMaterial',{userName:userName}) }}">Añadir nuevo ejercicio</option>
					<option value="{{ path('administrarMaterial',{userName:userName}) }}">Añadir nueva máquina</option>
				</select>
		</div>
	</nav>
	<div class="container">
		{{form_start(form1)}}
		<div>
			{{ form_label(form1.nombreEjercicio, 'Introduce el nombre')
			}}

			{{ form_widget(form1.nombreEjercicio)}}
		</div>
		<div>
			{{ form_label(form1.descripcion, 'Introduce la descripción',) }}


			{{ form_widget(form1.descripcion)}}
		</div>
		<div>
			{{ form_label(form1.grupoMuscular, 'Introduzca el grupo muscular',)
			}}

			{{ form_widget(form1.grupoMuscular)}}
		</div>
        <div>
			{{ form_label(form1.maquina, 'Introduzca la máquina',)
			}}

			{{ form_widget(form1.maquina)}}
		</div>
        
		{{form_end(form1)}}

        {{form_start(form2)}}
		<div>
			{{ form_label(form2.nombre, 'Introduce el nombre')
									}}

			{{ form_widget(form2.nombre)}}
		</div>
		<div>
			{{ form_label(form2.marca, 'Introduce la marca') }}


			{{ form_widget(form2.marca)}}
		</div>
		<div>
			{{ form_label(form2.imagen, 'Introduzca número de repeticiones por serie',)
			}}

			{{ form_widget(form2.imagen)}}
		</div>
		{{form_end(form2)}}
	</div>
	</div>

{% endblock %}
